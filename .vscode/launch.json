{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "pwa-chrome",
      "request": "launch",
      "name": "Launch Chrome against localhost",
      "url": "http://localhost:4200",
      "webRoot": "${workspaceFolder}"
    },
    {
      "type": "node-terminal",
      "name": "Run Script: start",
      "request": "launch",
      "command": "npx nx build adminn --skip-cache",
      "cwd": "${workspaceFolder}"
    },
    {
      "type": "node-terminal",
      "name": "Serve simple",
      "request": "launch",
      "command": "npx nx serve simple --skip-cache",
      "cwd": "${workspaceFolder}"
    }
  ]
}
